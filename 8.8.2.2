//Diese Aufgabe wurde mit MT2_KG_10 im Praktikum in der Hochschule gelöst.

#include <iostream>
#include <thread>

using namespace std;

thread thr;

void Ausgabe() {
	thread::id idR = thr.get_id();
	cout << idR << endl;
}
int main() {
	thr = thread(Ausgabe);
	thread::id idD = thr.get_id();
	cout << "Informatik 2 - Betriebsysteme - Praktikum 1 Aufgabe 2\n";
	cout << "Die Thread ID lautet: " << thr.get_id();
	thr.join();
	
	system("pause");
	return 0;
}
